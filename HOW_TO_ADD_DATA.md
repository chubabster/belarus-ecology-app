# 📝 Инструкция по заполнению базы данных

Если вы использовали **init_empty.sql** для создания пустой базы данных, эта инструкция поможет вам добавить первые данные.

## 🎯 Три способа добавления данных

### 1️⃣ Через веб-интерфейс (самый простой)

После запуска приложения откройте http://localhost:8000 и:

1. **Добавить идею:**
   - Нажмите кнопку "Добавить идею" в навигации
   - Заполните форму
   - Нажмите "Отправить идею"

2. **Просмотр статистики:**
   - На главной странице будет видна статистика
   - Идеи отобразятся в разделе "Идеи людей"

### 2️⃣ Через API (для программистов)

#### Добавить проблему:

```bash
curl -X POST http://localhost:3000/api/problems \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Загрязнение водоёмов",
    "description": "Реки и озёра Беларуси страдают от промышленных и бытовых сточных вод",
    "category": "Вода",
    "severity": "высокая"
  }'
```

#### Добавить решение:

Сначала узнайте ID проблемы:
```bash
curl http://localhost:3000/api/problems
```

Затем добавьте решение:
```bash
curl -X POST http://localhost:3000/api/solutions \
  -H "Content-Type: application/json" \
  -d '{
    "problem_id": 1,
    "title": "Участие в очистке водоёмов",
    "description": "Присоединяйтесь к волонтёрским акциям по очистке берегов рек и озёр",
    "level": "индивидуальный",
    "difficulty": "легко",
    "impact": "средний"
  }'
```

#### Добавить идею:

```bash
curl -X POST http://localhost:3000/api/ideas \
  -H "Content-Type: application/json" \
  -d '{
    "author_name": "Александр Иванов",
    "title": "Создание экотроп в городских парках",
    "description": "Предлагаю создать образовательные экологические тропы в парках Минска",
    "category": "Образование"
  }'
```

### 3️⃣ Через SQL (напрямую в БД)

Если хотите сразу добавить много данных:

```bash
psql -U postgres -d belarus_ecology
```

```sql
-- Добавить проблему
INSERT INTO problems (title, description, category, severity) 
VALUES (
  'Загрязнение водоёмов',
  'Реки и озёра Беларуси страдают от промышленных и бытовых сточных вод',
  'Вода',
  'высокая'
);

-- Добавить решение (замените 1 на ID вашей проблемы)
INSERT INTO solutions (problem_id, title, description, level, difficulty, impact)
VALUES (
  1,
  'Участие в очистке водоёмов',
  'Присоединяйтесь к волонтёрским акциям по очистке берегов рек и озёр',
  'индивидуальный',
  'легко',
  'средний'
);

-- Добавить идею
INSERT INTO ideas (author_name, title, description, category)
VALUES (
  'Александр Иванов',
  'Создание экотроп в городских парках',
  'Предлагаю создать образовательные экологические тропы в парках Минска',
  'Образование'
);
```

## 📋 Допустимые значения для полей

### Категории (problems и ideas):
- `Вода`
- `Леса`
- `Воздух`
- `Отходы`
- `Радиация`
- `Почва`
- `Энергия`
- `Транспорт`
- `Образование`
- `Другое`

### Уровень серьёзности (problems):
- `низкая`
- `средняя`
- `высокая`
- `критическая`

### Уровень решения (solutions):
- `индивидуальный`
- `общественный`
- `государственный`

### Сложность решения (solutions):
- `легко`
- `средне`
- `сложно`

### Влияние решения (solutions):
- `низкий`
- `средний`
- `высокий`

### Статус идеи (ideas):
- `новая` (по умолчанию)
- `на рассмотрении`
- `одобрена`
- `отклонена`
- `реализована`

## 🔄 Импорт готовых данных

Если передумали и хотите загрузить примеры данных:

```bash
# Удалите пустые таблицы и создайте с данными
psql -U postgres -d belarus_ecology -f backend/database/init.sql
```

Это перезапишет структуру и добавит 6 проблем и 15+ решений.

## ✅ Проверка данных

Посмотреть что добавлено:

```bash
# Через API
curl http://localhost:3000/api/problems
curl http://localhost:3000/api/solutions
curl http://localhost:3000/api/ideas

# Через psql
psql -U postgres -d belarus_ecology -c "SELECT * FROM problems;"
psql -U postgres -d belarus_ecology -c "SELECT * FROM solutions;"
psql -U postgres -d belarus_ecology -c "SELECT * FROM ideas;"
```

## 💡 Советы

1. **Начните с идей** - их проще всего добавлять через веб-интерфейс
2. **Добавьте 1-2 проблемы** через API для тестирования
3. **Привяжите к ним решения** чтобы увидеть связи
4. **Проверьте статистику** на главной странице

## 🆘 Проблемы?

Если что-то не работает:
1. Проверьте, что сервер запущен: http://localhost:3000
2. Проверьте логи в консоли браузера (F12)
3. Проверьте логи backend сервера
4. Убедитесь, что PostgreSQL работает

---

**Удачи в заполнении базы данных! 🌱**
